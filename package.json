{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "lerna run start --parallel",
    "new-version": "lerna version --conventional-commits --yes"
  }
}
