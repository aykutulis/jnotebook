{
  "name": "jpen",
  "version": "1.0.1",
  "description": "A CLI to launch an interactive development environment for writing and documenting code",
  "bin": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild --target=node12 src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "Aykut Ulış",
  "license": "ISC",
  "dependencies": {
    "@jpen/local-client": "^1.0.1"
  },
  "devDependencies": {
    "@jpen/local-api": "^1.0.1",
    "@types/node": "^16.6.1",
    "commander": "^8.1.0",
    "esbuild": "^0.12.21",
    "typescript": "^4.3.5"
  }
}
